#include <stdio.h>

// Define constants using #define
#define PI 3.14159
#define MAX_LENGTH 100

int main() {
    double radius = 5.0;
    double area = PI * radius * radius;

    printf("Area of the circle: %f\n", area);  // Output: Area of the circle: 78.539750

    return 0;
}
